Show me all the implementation modules and help me explore them.

1. Run `jetstackai modules list` to get all modules.

2. Summarize:
   - Total number of modules
   - For each module: name, description, total asset count, asset breakdown by type

3. Ask if I want to:
   - View full details of a module (`jetstackai modules get <id>`)
   - View the module index (`jetstackai modules index <id>`) — this shows the structured documentation of assets, dependencies, mutable vs immutable fields
   - Create a new module
   - Deploy a module to a portal (redirect to `/jetstack-deploy`)
