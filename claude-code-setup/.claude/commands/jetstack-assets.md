Show me all the assets in the JetStack AI library and help me explore them.

1. Run `jetstackai assets list` to get the full asset list.

2. Summarize the library:
   - Total number of assets
   - Breakdown by type (workflows, forms, emails, lists, pipelines, pages, templates, blogPosts, hubdbTables)
   - Breakdown by source portal
   - List the most recent assets

3. Present the summary in a clean table format.

4. Ask if I want to:
   - View details of a specific asset (`jetstackai assets get <id>`)
   - View the raw HubSpot schema of an asset (`jetstackai assets schema <id>`)
   - Search for assets by name
   - Delete or rename any assets
